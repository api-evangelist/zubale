{
  "opencollection": "1.0.0",
  "info": {
    "name": "Zubale API Documentation for External Notification Handler Webhook: Payload Structure for Order Notification API",
    "version": "v3"
  },
  "request": {
    "auth": {
      "type": "bearer",
      "token": "{{bearerToken}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "Webhook: Payload Structure for Order Notification",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Webhook Configuration",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://api.zubale.com/webhook",
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "Configuration for setting up the webhook. The client should point their webhook to the provided URL with the appropriate method and headers."
        }
      ]
    }
  ],
  "bundled": true
}